# Current UI screenshots

This directory is reserved for screenshots used by both `README.md` and
`README_EN.md`.

Expected files:

| File | Suggested content |
| --- | --- |
| `main-workspace.png` | Full desktop view after loading a log, including the chart and AI panel |
| `provider-configuration.png` | Provider configuration dialog with non-sensitive example values |
| `field-selection.png` | Searchable message and field selection dialog |
| `ai-diagnostic-report.png` | Completed AI report beside a populated time-series chart |

Use a consistent browser viewport where practical. Before capturing the
Provider dialog, remove or mask API keys, private endpoints, device IDs,
coordinates, and other sensitive flight data.
